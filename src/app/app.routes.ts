import { Routes } from '@angular/router';
import { AnnouncementTableComponent } from './announcement-table/announcement-table.component';

export const routes: Routes = [
    {path: '', component: AnnouncementTableComponent}
];
